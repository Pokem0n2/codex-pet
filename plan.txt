根据hatch-pet中的规则，制作体积不超过30k的可分发、可离线独立运行的exe。
运行exe后会自动读取my-pet中所有宠物id，并在exe界面中出现id列表，用户点击列表中的id后，exe界面中会出现该宠物的状态预览，轮播9种状态的动画。用户每按下一次回车键，则在用户桌面范围内任意位置随机出现一个当前选中的宠物，允许出现多个宠物，允许选中的宠物重复创建并出现在桌面，生成数量无上限。优化响应速度，用户快速连击多次回车时，exe不应该出现阻塞、假死等现象。按ESC可以一次性关闭并退出exe，并退出所有已经生成的桌面宠物。
先做好exe制作的规划方案，然后用PowerShell输入gh auth status验证我已安装好的的GitHub CLI的可用性，接着用gh工具在我的GitHub上新建codex-pet项目，并按照规划的方案逐步骤实现exe功能，过程中注意push diff变更到GitHub，最终交付前记得审查代码漏洞、删除冗余代码、测试并验证功能、优化exe体积，并给我最终编译可用的exe